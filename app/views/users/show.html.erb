<%= stylesheet_link_tag('user-show')%>

<div id="user-details">
  <div class="user-description">
    <div class="img-div"><%= image_tag "" %></div>
    <div class="user-name">
      <h2><%= @user.name %></h2>
      <h6>Number of posts: <%= @user.posts_counter %></h6>
    </div>
  </div>
    <div class="bio-description">
      <h3>Bio</h3>
      <p><%= @user.bio%></p>
    </div>

  <% @user.recent_posts.each do |post| %>
    <div class="user-posts">
    <h3><%= post.title %></h3>
    <div><%= post.text %></div>
    <div class="comment-like">
      <span class="comment">Comments: 20</span> <span class="like"span>Likes: 10</span>
    </div>
  </div>
  <% end %>
  <div class="see-posts">
  <a href="/users/<%= @user.id %>/posts" id="user-btn">See all posts</a>
  </div>
</div>